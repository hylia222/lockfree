# 开发指南 —— 编译器优化、刷题路径、Git 节点、项目优先级

---

## 目录

1. [Debug vs Release 编译器优化详解](#1-debug-vs-release-编译器优化详解)
2. [LeetCode 手撕题单](#2-leetcode-手撕题单)
3. [GitHub 第一版提交时机](#3-github-第一版提交时机)
4. [项目后续工作优先级分析](#4-项目后续工作优先级分析)

---

## 1. Debug vs Release 编译器优化详解

### 1.1 你需要知道什么程度？

**基本原则**：不需要背细节，但需要知道编译器做什么、为什么能优化、做了优化后会怎样。

面试中常见问法：
> "Debug 和 Release 有什么区别？为什么 Release 跑得更快？"
> "写代码时要注意什么才能让编译器更好地优化？"

你能从原理层面回答清楚这两点就足够。

### 1.2 核心差异一句话

| | Debug | Release |
|---|---|---|
| **目标** | 方便调试 | 极致性能 |
| **代码体积** | 更大（保留调试信息） | 更小（剔除无用代码） |
| **执行速度** | 慢 | 快 3-10x |
| **可调试性** | 完美 | 部分断点失效 |

### 1.3 编译器到底做了什么优化（六大类）

#### ① 内联展开（Inlining）

```cpp
// 你写的代码
bool push(const T& item) {
    size_t w = write_pos_.load(memory_order_acquire);
    size_t r = read_pos_.load(memory_order_acquire);
    if ((w + 1) % Capacity == r) return false;
    buffer_[w] = item;
    write_pos_.store((w + 1) % Capacity, memory_order_release);
    return true;
}

// Debug：保留函数调用
// 调用处：call push    ← 压栈、跳转、返回，约 20+ 条指令

// Release：内联展开
// 调用处：直接展开为 6 条指令    ← 无调用开销
```

**效果**：消除函数调用开销（压栈/call/ret）。

#### ② 寄存器分配（Register Allocation）

```cpp
// Debug：每次从内存读/写 write_pos_
//   mov  rax, [write_pos_]    ← 读内存，约 60-80 ns
//   add  rax, 1
//   mov  [write_pos_], rax    ← 写内存

// Release：w 一直放在寄存器里
//   add  ebx, 1               ← 改寄存器，约 0.3 ns
```

**效果**：消除 99% 的内存访问，寄存器比 L1 缓存快 10 倍。

#### ③ 常量传播与折叠（Constant Propagation）

```cpp
size_t next = (w + 1) % Capacity;
// Capacity 是编译期常量（模板参数）
// 如果 Capacity = 1024，且是 2 的幂次方：
//   (w + 1) % 1024  →  (w + 1) & 1023     ← 取模变成按位与
//   Debug：用 div 指令（除法，~25 个周期）
//   Release：用 and 指令（按位与，~1 个周期）
```

**效果**：除法→位运算，循环不变式提到循环外。

#### ④ 死代码消除（Dead Code Elimination）

```cpp
// 你代码中可能存在的"写了但没用的代码"
int unused = compute_something();   // ← Release 直接消失

// assert 在 Release 下消失
assert(write_pos_ < Capacity);      // ← Release 被删除
```

**效果**：只保留实际影响输出的代码。

#### ⑤ 循环优化（Loop Optimization）

你的压测循环：
```cpp
while (sent < TOTAL_MSGS) {
    if (q.push(42)) sent++;
    else PAUSE();
}
```

- **Debug**：每次循环都检查 `sent < TOTAL_MSGS`、调用 `push` 并检查返回值
- **Release**：循环展开 + 软件流水线，一次迭代处理多条消息

#### ⑥ 指令重排（Instruction Reordering）

```cpp
// 你写的代码
buffer_[w] = item;             // 1. 写数据
write_pos_.store(...);         // 2. 更新指针
// CPU 在 Release 下可以：
// - 在写 buffer_[w] 之前就预加载 write_pos_ 的地址
// - 将无关指令调度到加载的空闲周期中
```

**效果**：充分利用 CPU 的指令级并行，一个周期执行多条指令。

### 1.4 你的项目差距实测

```
Debug:   6-7 M msg/s
Release: 15-19 M msg/s
提升:    ~3x
```

**为什么不是 10x？** 因为你的核心瓶颈不在 CPU 指令数，而在**缓存一致性（Cache Coherency）**：

```
生产者核心写 buffer_[w]
   → 写操作使消费者核心的缓存行失效
   → 消费者核心必须重新从 L3/内存加载
   → 一次缓存同步 ≈ 60-80 ns

你的队列 Release 下一条消息 ≈ 62 ns
  = 60 ns 缓存同步 + 2 ns 实际执行
  → 再优化 CPU 指令也只能提升最后那 2 ns
```

### 1.5 面试回答模板

> **Q: Debug和Release有什么区别？**
>
> "Debug 模式保留完整的调试信息、不做优化，方便打断点和单步调试。
> Release 模式会进行内联展开、寄存器分配、常量传播、死代码消除等六大类优化，
> 性能通常比 Debug 快 3-10 倍。"
>
> **Q: 你的项目 Release 只提升了 3 倍，为什么？**
>
> "因为我的项目瓶颈不在 CPU 指令数，而在于两个核心之间的缓存同步延迟。
> 即使把 CPU 指令优化到极致，缓存同步一次仍需要 60-80 ns，
> 这成了硬件的天花板。如果想突破，需要换思路——比如批处理来减少同步次数。"

### 1.6 推荐深入学习资料

如果你真想深入学：

| 资料 | 说明 | 建议阅读时长 |
|---|---|---|
| **《CSAPP》第5章**（优化程序性能） | 教科书级别，从硬件讲编译器优化原理 | 3-4 小时 |
| **Agner Fog 的优化手册** | 真正的底层优化圣经，讲 CPU 微架构 | 参考查阅 |
| **Matt Godbolt 的 Compiler Explorer** | 在线看编译器生成的汇编 | 动手实验用 |
| **《系统级性能优化》**（Intel 官方手册） | 缓存、流水线、分支预测 | 选读 |

---

## 2. LeetCode 手撕题单

### 2.1 刷题策略

你的项目方向 = **C++ + 无锁编程/并发/内存管理**

面试重点：
1. **手写数据结构**（链表、队列、栈、哈希表、二叉树）
2. **C++ 特有的考察点**（智能指针、RAII、移动语义）
3. **并发相关**（但一般不会直接考无锁，会考生产者-消费者）
4. **算法**（贪心、二分、DFS/BFS、动态规划）

### 2.2 题单（按优先级从高到低）

#### 第一阶段：数据结构基本功（20 题）

这几题直接涉及你项目的知识，优先刷：

| # | 题号 | 题目 | 为什么重要 | 建议用时 |
|---|---|---|---|---|
| 1 | **707** | 设计链表 | 手写链表基础，指针操作基本功 | 20 min |
| 2 | **155** | 最小栈 | 用两个栈实现 O(1) 取最小 | 15 min |
| 3 | **232** | 用栈实现队列 | 两个栈模拟队列，和你项目直接相关 | 15 min |
| 4 | **225** | 用队列实现栈 | 反过来练 | 15 min |
| 5 | **622** | 设计循环队列 | 和你的 SPSC 队列几乎一样，数组+头尾指针 | 20 min |
| 6 | **641** | 设计循环双端队列 | 队列的进阶 | 20 min |
| 7 | **706** | 设计哈希映射 | 哈希表 + 链表解决冲突 | 25 min |
| 8 | **146** | LRU 缓存 | 哈希表 + 双向链表，经典高频题 | 30 min |
| 9 | **460** | LFU 缓存 | LRU 的难度升级版 | 35 min |
| 10 | **208** | 实现 Trie（前缀树） | 树形结构，面试常考 | 20 min |
| 11 | **211** | 添加与搜索单词 | Trie 进阶 | 20 min |
| 12 | **295** | 数据流的中位数 | 两个堆，经典设计题 | 20 min |
| 13 | **380** | O(1) 时间插入、删除和获取随机元素 | 数组+哈希表 | 20 min |
| 14 | **381** | O(1) 时间插入、删除和获取随机元素（允许重复） | 上面那题的进阶 | 25 min |
| 15 | **346** | 数据流中的移动平均值 | 队列简单应用 | 10 min |
| 16 | **353** | 贪吃蛇 | 队列+双端队列应用 | 25 min |
| 17 | **933** | 最近的请求次数 | 队列应用 | 10 min |
| 18 | **1700** | 无法吃午餐的学生数量 | 队列模拟 | 15 min |
| 19 | **138** | 复制带随机指针的链表 | 链表 + 哈希表深拷贝 | 25 min |
| 20 | **430** | 扁平化多级双向链表 | 链表 + DFS | 25 min |

**每周 5 题，4 周刷完。**

#### 第二阶段：并发与锁相关（8 题）

| # | 题号 | 题目 | 原因 |
|---|---|---|---|
| 21 | **1114** | 按序打印 | 线程同步基本题 |
| 22 | **1115** | 交替打印 FooBar | 两个线程交替执行 |
| 23 | **1116** | 打印零与奇偶数 | 三个线程协同 |
| 24 | **1117** | H2O 生成 | 信号量经典题 |
| 25 | **1195** | 交替打印字符串 | 多线程题综合 |
| 26 | **1226** | 哲学家进餐 | 死锁预防经典题 |
| 27 | **1242** | 多线程网页爬虫 | 并发爬虫设计 |
| 28 | **1279** | 红绿灯路口 | 并发交通控制 |

**这 8 题做完，并发相关面试基本不虚。**

#### 第三阶段：算法（30 题）

| 分类 | 题号 | 题目 |
|---|---|---|
| **二分** | 704、34、33、153 | 二分查找、旋转数组 |
| **双指针** | 167、15、11、42 | 接雨水（经典） |
| **滑动窗口** | 3、76、239 | 无重复子串、最小覆盖、滑动窗口最大值 |
| **DFS/BFS** | 200、207、994、542 | 岛屿数量、拓扑排序、腐烂橘子 |
| **二叉树** | 94、102、105、236、124 | 遍历、重建、最近祖先 |
| **堆** | 215、347、373 | TopK、前 K 高频元素 |
| **动态规划** | 70、198、300、1143、72 | 入门到编辑距离 |
| **图** | 133、207、210、684 | 克隆图、拓扑排序 |

### 2.3 刷题技巧

1. **先分类刷**：数组→链表→栈/队列→哈希表→树→动态规划
2. **每题只写 30 分钟**：超过 30 分钟没思路直接看题解
3. **用 C++ 刷**：你项目的语言，边刷边熟悉 STL
4. **二八原则**：刷完第一阶段 20 题 + 并发 8 题，90% 的面试够用了

---

## 3. GitHub 第一版提交时机

### 3.1 现在就能推

你的项目已经具备：

```
lockfree/
├── CMakeLists.txt          ← 可构建
├── include/
│   ├── spsc_queue.h        ← 核心无锁队列（完整实现）
│   └── utils.h             ← 工具（CACHE_ALIGNED、PAUSE）
├── examples/
│   ├── 02_spsc_demo.cpp    ← 示例（单线程+多线程）
│   └── 03_spsc_benchmark.cpp ← 基准测试
```

这是一个**完整可运行的最小版本**，已经值得提交。

### 3.2 提交前建议做

**① 清理 build 目录**
```
rmdir /s D:\lockfree\build
```
build 目录是编译产物，不应该提交。

**② 创建 .gitignore**
```
build/
*.exe
*.pdb
*.ilk
*.obj
*.tlog
.vs/
```

**③ 写 README.md**

```
# Lockfree —— 高性能无锁消息框架

一个用 C++17 实现的通用高性能消息框架，核心基于无锁数据结构。

## 已完成

- [x] SPSC (Single Producer Single Consumer) 无锁队列
  - Debug: ~7 M msg/s
  - Release: ~16 M msg/s

## 构建

cmake -S . -B build
cmake --build build --config Release
build/Release/spsc_bench.exe
```

**④ 一个合理的初始 commit message**

```
feat: SPSC 无锁队列初始实现

- 实现 SPSC（单生产者单消费者）无锁环形队列
- 使用 std::atomic + memory_order release/acquire 保证无锁
- 支持任意数据类型（模板化）
- 15-19 M msg/s（Release 模式，2 核压测）
```

### 3.3 推荐节点

| 提交版本 | 时机 | 内容 |
|---|---|---|
| **v0.1.0** | 现在 | SPSC 队列 + 基准测试 |
| **v0.2.0** | 加批处理/MPSC 后 | 多队列支持 |
| **v0.5.0** | 加 IO 引擎后 | 网络通信能力 |
| **v1.0.0** | 加 Req-Rep 协议后 | 第一个完整可用版本 |

你现在就可以推 **v0.1.0**。

---

## 4. 项目后续工作优先级分析

### 4.1 项目全景图

```
你已经完成（19%）
├── SPSC 无锁队列 (100%)     ✅
├── CMake 构建系统 (100%)    ✅
│
还没做（81%）
├── 批处理/批量操作  (0%)    ★ 立即可做
├── MPSC 队列        (0%)    ★ 重要
├── MPMC 队列        (0%)    ★★
├── RingBuffer 缓冲区 (0%)
├── 内存池           (0%)
├── IO 引擎 (epoll/kqueue) (0%)
├── 序列化层 (JSON/Protobuf) (0%)
├── 连接管理         (0%)
├── 协议层 (Req-Rep/Pub-Sub) (0%)
├── 语言绑定 (Python/Go/Rust) (0%)
├── 文档/监控/CI    (0%)
```

### 4.2 优先级矩阵

判断标准：
- **面试价值**：面试官最可能问什么
- **项目价值**：对项目架构的贡献
- **学习价值**：能学到多少新东西
- **难度**：做起来需要多少时间

| 方向 | 面试价值 | 项目价值 | 学习价值 | 难度 | **综合推荐** |
|---|---|---|---|---|---|
| **批处理优化** | ★★ | ★★★ | ★★ | ★☆ | **做，1-2 天** |
| **MPSC 队列** | ★★★★ | ★★★★ | ★★★★ | ★★★ | **最推荐** |
| **MPMC 队列** | ★★★★★ | ★★★★★ | ★★★★★ | ★★★★ | 最终目标 |
| **RingBuffer** | ★★★ | ★★★ | ★★ | ★★ | 可选 |
| **内存池** | ★★★★ | ★★★★ | ★★★ | ★★★ | 中优先级 |
| **IO 引擎** | ★★ | ★★★★★ | ★★★★ | ★★★★ | 远期 |
| **序列化层** | ★★ | ★★ | ★ | ★ | 低优先级 |
| **连接管理** | ★★ | ★★★★ | ★★★ | ★★★ | 中优先级 |
| **协议层** | ★★★ | ★★★★ | ★★★★ | ★★★★★ | 远期 |

### 4.3 推荐执行路线

```
第 1 阶段（1-2 周）：现有基础上深化
├── 批处理 push（1 天）→ 从 16M 冲到 40M
├── 基准测试完善（1 天）→ 延迟分布图
├── MPSC 队列（3-4 天）→ 多生产者支持
│
第 2 阶段（2-3 周）：核心数据结构完善
├── MPMC 队列（4-5 天）→ 最难的队列
├── 内存池实现（2 天）→ 减少内存碎片
├── 读写锁（1 天）→ 补充锁原语
│
第 3 阶段（2-3 周）：IO 层
├── EventLoop 封装（3 天）
├── TCP 传输（2 天）
├── 连接池（1 天）
│
第 4 阶段（持续）：协议与应用
├── 请求-响应模式
├── 发布-订阅模式
├── 序列化集成
```

### 4.4 我的建议：下一步做 MPSC

原因是：

1. **面试价值极高**：多生产者是面试常考场景
2. **项目自然延伸**：你做 SPSC 已经懂了基础，MPSC = SPSC + CAS 冲突解决
3. **学到无锁编程最核心的技术**：CAS 竞争解决、回退策略、内存回收（hazard pointer 或 epoch-based reclamation）
4. **和你的 SPSC 对比强烈**：可以从基准测试看出"真正的竞争"是什么效果

> **批处理优化**什么时候做？等做完 MPSC 再回头做。因为批处理优化是个技巧性的优化，而 MPSC 是**全新的数据结构设计**，学到的东西更多。
>
> 优先级排名：**MPSC > 批处理优化 > 内存池 > IO 引擎 > 协议层 > 序列化**

---

## 总结

| 问题 | 回答 |
|---|---|
| **Debug/Release 优化需要细学吗？** | 了解六大类优化 + 缓存一致性瓶颈 ≥ 面试够用。想深入研究读《CSAPP》第5章。 |
| **LeetCode 刷多少？** | 数据结构 20 题 + 并发 8 题 + 算法 30 题 = 58 题，覆盖 90% 面试。 |
| **GitHub 什么时候提交？** | **现在就能推** v0.1.0。加个 README 和 .gitignore 即可。 |
| **下一块工作做什么？** | **MPSC 队列**。价值最高，学到的东西最多。 |
